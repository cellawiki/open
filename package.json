{
  "name": "@cellawiki/open-source-monorepo",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "fmt": "prettier --write .",
    "dev": "vitest",
    "test": "vitest run",
    "check": "prettier --check . && vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "prettier-plugin-sh": "^0.14.0",
    "vite-node": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
